services:
  - type: web
    name: vio-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn vio_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11